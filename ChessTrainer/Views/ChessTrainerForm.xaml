<Window x:Class="ChessTrainer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessTrainer"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResizeWithGrip" WindowStyle="None" AllowsTransparency="True">
    
    <Window.Resources>
        <!--=========================================Стили==========================================-->

        <Style x:Key="MinimizeAndCloseButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="7,0"/>
            <Setter Property="Foreground" Value="WhiteSmoke"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}"></TextBlock>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="Gold"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="Yellow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SideMenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Height" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <StackPanel Width="250" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                                <ContentControl Width="50" Height="50"
                                   Margin="0 0 20 0" Content="{DynamicResource IconForSideMenuButton}"
                                   VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                <TextBlock VerticalAlignment="Center" Text="{TemplateBinding Content}"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="FontSize" Value="20"/>
                                <Setter Property="Background" Value="IndianRed"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#CD3C3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <!--=========================================Стили==========================================-->

    <!--=========================================Разметка==========================================-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="80" MaxHeight="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="SandyBrown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Grid.ColumnSpan="2"
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" FontSize="25" Foreground="White"
                       Text="Шахматный тренажер by Pekor"/>
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"                        
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="10">
                <Button Style="{StaticResource MinimizeAndCloseButtonStyle}">一</Button>
                <Button Style="{StaticResource MinimizeAndCloseButtonStyle}">X</Button>
            </StackPanel>
        </Grid>
        <Grid Background="#ed9121" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Background="#FF6347" Grid.Column="0">
                <StackPanel VerticalAlignment="Center" >
                    <Button Style="{StaticResource SideMenuButtonStyle}" Content="Координаты">
                        <Button.Resources>
                            <Image x:Key="IconForSideMenuButton" Source="/Pics/IconsForSideMenu/Сoordinates.png"/>
                        </Button.Resources>
                    </Button>
                    <Button Style="{StaticResource SideMenuButtonStyle}" Content="Белое и черное">
                        <Button.Resources>
                            <Image x:Key="IconForSideMenuButton" Source="/Pics/IconsForSideMenu/BlackAndWhite.png"/>
                        </Button.Resources>
                    </Button>
                    <Button Style="{StaticResource SideMenuButtonStyle}" Content="Озвучка партии">
                        <Button.Resources>
                            <Image x:Key="IconForSideMenuButton" Source="/Pics/IconsForSideMenu/GameVoiceActing.png"/>
                        </Button.Resources>
                    </Button>
                    <Button Style="{StaticResource SideMenuButtonStyle}" Content="Сможет ли побить?">
                        <Button.Resources>
                            <Image x:Key="IconForSideMenuButton" Source="/Pics/IconsForSideMenu/IsCanBeat.png"/>
                        </Button.Resources>
                    </Button>
                    <Button Style="{StaticResource SideMenuButtonStyle}" Content="Преимущество">
                        <Button.Resources>
                            <Image x:Key="IconForSideMenuButton" Source="/Pics/IconsForSideMenu/MaterialAdvantage.png"/>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
<!--=========================================Разметка==========================================-->
